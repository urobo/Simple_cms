<%= link_to("<< Back to List", {:action => 'list'},:class => 'back-link') %>
<div class="Admin User delete">
	<h2>Delete Admin User</h2>
	<%= form_for(:admin_user, :url => {:action => 'destroy', :id => @admin.id}) do |f| %>
	<p>Are you sure you want to permanently delete this Admin User?</p>
	<p class="reference-name">
		<%= @admin.name %>
	</p>
	<div clas="form-buttons">
		<%= submit_tag("Delete Admin User") %>
	</div>
	<% end %>
</div>